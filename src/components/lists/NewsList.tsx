import { News } from "@prisma/client";
import React from "react";

const NewsList = ({ news = [], title }: { news: News[]; title: string }) => {
  return <section></section>;
};

export default NewsList;
